# Dev/test dependencies. Web/template tests (admin UI, i18n) need the server stack,
# so this now includes fastapi/jinja2/babel in addition to the pure-logic deps.
pytest==8.*
httpx==0.28.*
pydantic-settings==2.7.*
fastapi==0.115.*
jinja2==3.1.*
babel==2.*
python-multipart==0.0.*
phonenumbers==8.*
aiosqlite==0.20.*
pytest-timeout==2.*
# modem tests (PDU send, CDS aggregation) import app.modem.at_commands → serial_asyncio
pyserial==3.5.*
pyserial-asyncio==0.6.*
